# 抖音 9 图自动化 · 操作说明

> 目标：每天自动拉热榜 → 过滤敏感内容 → 生成 9 张可直接发的卡片图，你只管挑图发布。
> 落点站：https://newsnow-yin.pages.dev （自建 NewsNow，已在跑）

---

## 一、每天发生什么（08:00 自动）

1. 定时任务触发脚本 → 从自建站拉 12 个安全源热榜（百度/抖音/酷安/豆瓣/少数派/IT之家/什么值得买/虎扑/懂球帝/爱奇艺/快手/百度贴吧）
2. 按合规黑名单过滤**时政 / 国际冲突 / 伤亡突发事件 / 政策能源类**
3. 跨源精选 7 条（保证来源多样，不挤在一个平台）
4. 生成 9 张 1080×1440 卡片 PNG（封面 + 7 内容卡 + 结尾落点卡）
5. 同时写出 `topics.txt`（选题清单，发布前快速过一眼）

**产出目录**：`douyin_cards/YYYY-MM-DD/card_01.png … card_09.png` + `topics.txt`

---

## 二、手动运行

```bash
/Users/yin/.workbuddy/binaries/python/envs/default/bin/python \
  /Users/yin/WorkBuddy/热搜小程序/generate_douyin_cards.py
```
可选参数：`--date YYYY-MM-DD`（默认今天）、`--out 自定义目录`
> 需要联网（脚本从 newsnow-yin.pages.dev 拉数据），运行时放开沙箱网络。

---

## 三、管理定时任务

- 任务名：**每日抖音热榜9图生成**，每天 08:00 触发
- 暂停 / 删除 / 改时间：WorkBuddy 设置 → 自动化（Automations）→ 找到该任务
- 想改发布时间（如改成 07:30 或 21:00）：编辑任务的 RRULE（如 `FREQ=DAILY;BYHOUR=7;BYMINUTE=30`）

---

## 四、每天早上你要做的（约 1 分钟）

1. 打开 `douyin_cards/当天日期/topics.txt` 扫一眼 7 条选题
2. 若某条仍偏硬/不想发，手动删对应 `card_0X.png` 或换图
3. 把 9 张图按顺序发到抖音图文（需 <7 条时脚本会提示人工补）
4. 评论区置顶落点话术："全平台热榜戳我主页链接 🔗"

---

## 五、可调参数（脚本顶部）

| 变量 | 含义 | 默认 |
|---|---|---|
| `SAFE_SOURCES` | 拉取的数据源清单 | 12 个科技/影视/体育/生活源 |
| `SENSITIVE` | 命中即丢弃的敏感词黑名单 | 党政/国际/伤亡/政策等 |
| `SELECT_N` | 精选条数 | 7 |
| `PER_SOURCE` | 每源候选上限 | 10 |
| `TIMEOUT` | 单源超时（秒） | 15 |
| `ACCENT` / `HOOKS` | 卡片配色、各类目点评话术 | 蓝/粉/绿/橙 |

> 新增安全源：先确认该源类目安全，再把 id 加进 `SAFE_SOURCES` 并在 `SOURCE_NAME` / `CATEGORY` 里补映射。

---

## 六、已知坑 / 维护提醒

- **个别源偶发超时**：什么值得买、快手今天出现过 `未取到`，属上游限流，不影响其他源；脚本会跳过并告警。
- **黑名单要常维护**：热榜会出现新形式政策/突发事件词，若发现漏网之鱼，把它加进 `SENSITIVE` 即可。
- **落点站必须在线**：脚本依赖 newsnow-yin.pages.dev；若站点被你删了或欠费，脚本会拿不到数据。站点维护见《NewsNow自部署实操步骤.md》。
- **抖音禁帖内直链**：落点靠 bio 文字 + 评论置顶，千粉后 bio 链接才解锁。
- **字体依赖**：依赖 macOS 系统字体 `/System/Library/Fonts/Hiragino Sans GB.ttc`；换机器需确认字体路径。

---

## 七、文件清单

- `generate_douyin_cards.py` — 生成脚本（核心）
- `douyin_cards/YYYY-MM-DD/` — 每日产出（PNG + topics.txt）
- 关联文档：`抖音热榜号运营SOP.md`、`抖音首日9图内容模板.md`、`NewsNow自部署实操步骤.md`
